# Third-party notices

## Bundled arena assets

`assets/arenas/` と `assets/models/` の YAML、SDF、STL は
元のシミュレーション資産から必要な部分を取り込んでいます。
元資産の `package.xml` はライセンスを `BSD` と宣言しています。

`collision_parts/` の OBJ は、上記 STL から CoACD 1.0.11 を用いて生成した派生資産です。
元資産と同じ条件に従います。

配布時には、本ファイル、`LICENSE`、`assets/README.md` を資産とともに保持してください。
